package ru.stqa.mantis.manager;

import ru.stqa.mantis.model.UserDate;

import java.time.Duration;
import java.util.regex.Pattern;

public class User extends HelperBase {
    public User(ApplicationManager manager) {
        super(manager);
    }
    public void startRegistration (UserDate user){

    }
}
